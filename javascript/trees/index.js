'use strict';
const { Tree, BST } = require('../trees/tree')
const Node = require('../trees/node')
const treeToString = require('tree-to-string')
let bst = new Tree();

 bst.root = new Node(1, new Node(4, new Node(7), new Node(2)), new Node(3, new Node(9), new Node(6)));






